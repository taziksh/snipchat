import pytest

#TODO: resolve directory structure
"""
from api import app, api


@pytest.fixture
def client():
    with app.test_client() as client:
        yield client

def test_api_response(client):
    response = client.post('/snippet', json={
        'phrase': "wait for it"
    })
    data = response.json
"""

